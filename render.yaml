services:
  - type: web
    name: ping-pong-server
    runtime: docker
    dockerfilePath: ./server/Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: HEARTBEAT_SECRET
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
